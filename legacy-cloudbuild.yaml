# LEGACY - This file is no longer used. Deployment now uses GitHub Actions (.github/workflows/deploy.yml)
# Kept for reference only

steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ldr-ai:$COMMIT_SHA', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ldr-ai:$COMMIT_SHA']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ldr-ai'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ldr-ai:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '2'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'OPENAI_API_KEY=${_OPENAI_API_KEY},GOOGLE_DRIVE_API_KEY=${_GOOGLE_DRIVE_API_KEY},GOOGLE_VISION_API_KEY=${_GOOGLE_VISION_API_KEY},SUPABASE_URL=${_SUPABASE_URL},SUPABASE_ANON_KEY=${_SUPABASE_ANON_KEY},ENVIRONMENT=production,APP_URL=https://ldr.clocknumbers.com'
      - '--tag'
      - '${_ENV_NAME}'

images:
  - 'gcr.io/$PROJECT_ID/ldr-ai:$COMMIT_SHA'
